import {StyleSheet} from 'react-native';

/* Const */
import {ErrorColor} from '../../const/Settings';

const ErrorStyle = StyleSheet.create({
  textError: {
    marginLeft: 15,
    color: ErrorColor,
    fontWeight: 'bold',
    fontSize: 15,
  },
});

export default ErrorStyle;
