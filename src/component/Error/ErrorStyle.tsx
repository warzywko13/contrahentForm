import {StyleSheet} from 'react-native';

const ErrorStyle = StyleSheet.create({
  textError: {
    marginLeft: 15,
    color: 'red',
    fontWeight: 'bold',
    fontSize: 15,
  },
});

export default ErrorStyle;
